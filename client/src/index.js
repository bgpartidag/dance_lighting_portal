import React from 'react';
import ReactDOM from 'react-dom';
import Main from './components/Main';

// Takes code from main and puts into root
// Renders code into website

ReactDOM.render(
  <Main/>,
  document.getElementById('root')
);
